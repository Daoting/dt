<Page
    x:Class="Infras.Demo.ExportPdfPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:a="using:Dt.Base">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Padding="10" Spacing="20">
            <Button Content="返回" Click="OnBack" />
            <Button Content="打开" Click="OpenFile" />
            <Button Content="保存" Click="OnSave" />
            <Button Content="插入图片" Click="AddPictureButton_Click" />
            <Button Content="base64图片" Click="AddBase64Pic" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Padding="10" Spacing="20" Grid.Row="1">
            <Button Content="导出Pdf" Click="OnExport" />
            <Button Content="选择区域导出Pdf" Click="OnExportArea" />
            <Button Content="默认打印" Click="OnPrint" />
            <Button Content="0边距打印" Click="OnPrint0" />
            <Button Content="打印选择区域" Click="OnPrintArea" />
        </StackPanel>
        <Border x:Name="_snap" Grid.Row="2" />
        <a:Excel x:Name="_excel" Grid.Row="2" />
    </Grid>
</Page>
