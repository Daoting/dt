<a:List
    x:Class="Demo.Crud.虚拟List"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:a="using:Dt.Base"
    Title="列表">

    <a:Lv x:Name="_lv">
        <a:Cols>
            <a:Col ID="主表名称" />
            <a:Col ID="限长4" />
            <a:Col ID="不重复" />
            <a:Col ID="扩展1名称" />
            <a:Col ID="禁止选中" />
            <a:Col ID="禁止保存" />
            <a:Col ID="扩展2名称" />
            <a:Col ID="禁止删除" />
            <a:Col ID="值变事件" />
        </a:Cols>
    </a:Lv>
</a:List>
