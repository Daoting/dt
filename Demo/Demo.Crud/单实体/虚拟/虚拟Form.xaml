<a:Form
    x:Class="Demo.Crud.虚拟Form"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:a="using:Dt.Base"
    Title="表单">

    <a:Fv x:Name="_fv" Create="Add" Delete="Delete" Save="Save">
        <a:CText ID="主表名称" />
        <a:CText ID="限长4" />
        <a:CText ID="不重复" />
        <a:CText ID="扩展1名称" />
        <a:CBool ID="禁止选中" />
        <a:CBool ID="禁止保存" />
        <a:CText ID="扩展2名称" />
        <a:CBool ID="禁止删除" />
        <a:CText ID="值变事件" />
    </a:Fv>
</a:Form>