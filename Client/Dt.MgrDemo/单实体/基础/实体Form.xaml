<a:Mv
    x:Class="Dt.MgrDemo.单实体.实体Form"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:a="using:Dt.Base"
    Title="表单">

    <a:Mv.Menu>
        <a:Menu>
            <a:Mi ID="保存" Icon="保存" ShowInPhone="Icon" Click="OnSave" />
            <a:Mi ID="增加" Icon="加号" ShowInPhone="Icon" Click="OnAdd" />
            <a:Mi ID="删除" Icon="删除" Click="OnDel" />
        </a:Menu>
    </a:Mv.Menu>

    <a:Fv x:Name="_fv">
        <a:CNum ID="序列" IsInteger="True" />
        <a:CText ID="限长4" />
        <a:CText ID="不重复" />
        <a:CBool ID="禁止选中" />
        <a:CNum ID="校验后台列" IsInteger="True" />
        <a:CBool ID="禁止保存" />
        <a:CBool ID="禁止删除" />
        <a:CText ID="值变事件" />
        <a:CDate ID="创建时间" />
        <a:CDate ID="修改时间" />
    </a:Fv>
</a:Mv>