<a:Win
    x:Class="Dt.MgrDemo.多对多.角色Win"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:a="using:Dt.Base"
    xmlns:l="using:Dt.MgrDemo.多对多">

    <l:角色List x:Name="_mainList" Order="1" a:Ex.Dock="Left" />
    <l:角色Form x:Name="_mainForm" />
    <a:Tabs a:Ex.Dock="Right">
        <l:角色用户List x:Name="_用户List" />
        <l:角色权限List x:Name="_权限List" />
    </a:Tabs>
</a:Win>
