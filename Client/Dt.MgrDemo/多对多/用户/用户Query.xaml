<a:Tab
    x:Class="Dt.MgrDemo.用户Query"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:a="using:Dt.Base"
    Title="查询"
    Icon="漏斗">

    <a:QueryFv x:Name="_fv">
            <a:CText ID="手机号" Query="Editable" />
            <a:CText ID="姓名" Query="Editable" />
            <a:CText ID="密码" Query="Editable" />
        <CheckBox Content="筛选条件之间【与】关系" IsChecked="{Binding IsAnd,ElementName=_fv,Mode=TwoWay}" />
    </a:QueryFv>
</a:Tab>