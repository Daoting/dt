<a:Dlg
    x:Class="Dt.Base.AudioRecordDlg"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:a="using:Dt.Base">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="_tbTimer" Text="00:00" Foreground="Red" FontSize="32" HorizontalAlignment="Center" />
        <Grid Width="60" Height="60" HorizontalAlignment="Center" Margin="0,10,0,0" Grid.Row="1">
            <Ellipse Fill="Red" IsHitTestVisible="False" />
            <TextBlock Text="&#xE082;" Foreground="White" FontSize="30" FontFamily="{StaticResource IconFont}" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0" Grid.Row="2">
            <Button Content="完　成" Click="OnSend" Background="{StaticResource 主蓝}" Foreground="White" Width="100" />
            <Button Content="放　弃" Click="OnCancel" Background="{StaticResource 深灰2}" Foreground="White" Width="100" Margin="20,0,0,0" />
        </StackPanel>
    </Grid>
</a:Dlg>
